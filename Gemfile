source "https://rubygems.org"

# The Jekyll gem, which is required to build the site
gem "jekyll", "~> 4.2"

# Optional: if you're using a theme like 'jekyll-theme-minimal'
gem "jekyll-theme-minimal"
